// lists all the content inside the folder - ls
// cd <directory name> - change the directory (folder name)
// clear or cls - clear the terminal
// cd .. - back to directory
// mkdir - for creating a folder
// dom - document object model

// <div attribute>==this function which are used to group of elements of the html files
// <a> tag=anchor tags which are used to join the  hyper links  to take them 
// <src>= those tag which are use image , script , viodio
// <src>=script tag which are used to link the javascript to html
<link>= link tag which are used to link the the css file to html file
// <href >= those tag which are used to copy the links ,webscripts
// <meta tag>=those tag which are used to data abot of data of webbrowers
